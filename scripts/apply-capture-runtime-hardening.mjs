import { readFile, writeFile } from 'node:fs/promises';

async function patch(path,replacements){
  let source=await readFile(path,'utf8');
  for(const [from,to] of replacements){
    if(!source.includes(from))throw new Error(`PATCH_ANCHOR_MISSING:${path}:${from.slice(0,80)}`);
    source=source.replace(from,to);
  }
  await writeFile(path,source,'utf8');
}

await patch('src/runtime.js',[
  ["import { navigationRisk } from './sandbox-policy.js';", "import { navigationRisk } from './sandbox-policy.js';\nimport { validateCaptureArtifact } from './capture-contract.js';"],
  ["let previewBootstrapId = null;\nconst activeWebMCPOperations", "let previewBootstrapId = null;\nlet captureMetadata = null;\nconst activeWebMCPOperations"],
  ["const CANCELLABLE_WEBMCP_TOOLS = new Set(['faultline_run','faultline_probe','faultline_reduce','faultline_autopilot']);\nconst revisions = new Map([['r1',{value:clone(fixture),pins:[]}]]);", "const CANCELLABLE_WEBMCP_TOOLS = new Set(['faultline_run','faultline_probe','faultline_reduce','faultline_autopilot','faultline_import_capture']);\nconst revisions = new Map([['r1',{value:clone(fixture),pins:[],capture:null}]]);"] ,
  ["function rememberRevision(rev,snapshot){ revisions.set(rev,clone(snapshot));trimRuntimeHistory(); }", "function rememberRevision(rev,snapshot){ const next={...clone(snapshot),capture:Object.hasOwn(snapshot,'capture')?clone(snapshot.capture):clone(captureMetadata)};revisions.set(rev,next);trimRuntimeHistory(); }"],
  ["return {version:3,store:persistedStore,axis,pins:[...pins],experimentLedger:clone(experimentLedger.slice(-profile.experiments)),revisions:[...revisions.entries()].slice(-profile.runtimeRevisions).map(([rev,snapshot])=>[rev,clone(snapshot)])};", "return {version:3,store:persistedStore,axis,pins:[...pins],capture:clone(captureMetadata),experimentLedger:clone(experimentLedger.slice(-profile.experiments)),revisions:[...revisions.entries()].slice(-profile.runtimeRevisions).map(([rev,snapshot])=>[rev,clone(snapshot)])};"],
  ["pins=new Set(Array.isArray(snapshot.pins)?snapshot.pins:[]);\n  experimentLedger=", "pins=new Set(Array.isArray(snapshot.pins)?snapshot.pins:[]);\n  captureMetadata=clone(snapshot.capture??null);\n  experimentLedger="],
  ["pins=new Set(Array.isArray(raw.pins)?raw.pins:[]);\n      experimentLedger=", "pins=new Set(Array.isArray(raw.pins)?raw.pins:[]);\n      captureMetadata=clone(raw.capture??null);\n      experimentLedger="],
  ["function inspect(){ const s=store.inspect(); return {revision:s.revision,case:s.value,pins:[...pins],unitCounts:{html:unitsFor('html',s.value.html).length,css:unitsFor('css',s.value.css).length,js:unitsFor('js',s.value.js).length},latest:experimentLedger.at(-1)||null,webmcp:!!document.modelContext}; }", "function inspect(){ const s=store.inspect(); return {revision:s.revision,case:s.value,capture:clone(captureMetadata),pins:[...pins],unitCounts:{html:unitsFor('html',s.value.html).length,css:unitsFor('css',s.value.css).length,js:unitsFor('js',s.value.js).length},latest:experimentLedger.at(-1)||null,webmcp:!!document.modelContext}; }"],
  ["function loadCase({expectedRevision=revision(),case:nextCase}){\n  validateCase(nextCase);\n  store.assertRevision(expectedRevision);\n  const before=snapshotCanonical();\n  pins.clear();\n  const result=store.commit(clone(nextCase),{kind:'case_load'},expectedRevision);\n  rememberRevision(result.revision,{value:clone(result.value),pins:[]});\n  persistMutation(before);\n  render();\n  renderPreview();\n  return inspect();\n}\nfunction resetCase", "function loadCase({expectedRevision=revision(),case:nextCase}){\n  validateCase(nextCase);\n  store.assertRevision(expectedRevision);\n  const before=snapshotCanonical();\n  pins.clear();\n  captureMetadata=null;\n  const result=store.commit(clone(nextCase),{kind:'case_load'},expectedRevision);\n  rememberRevision(result.revision,{value:clone(result.value),pins:[],capture:null});\n  persistMutation(before);\n  render();\n  renderPreview();\n  return inspect();\n}\nasync function importCapture({expectedRevision=revision(),artifact}={}, {signal}={}){\n  const normalized=validateCaptureArtifact(artifact);\n  validateCase(normalized.case);\n  store.assertRevision(expectedRevision);\n  throwIfAborted(signal);\n  const baseline=await runCase(normalized.case,{signal});\n  throwIfAborted(signal);\n  store.assertRevision(expectedRevision);\n  if(baseline.status!=='FAIL')throw new Error(`CAPTURE_BASELINE_NOT_FAILING:${baseline.status}`);\n  const before=snapshotCanonical();\n  pins.clear();\n  captureMetadata={format:normalized.format,version:normalized.version,capturedAt:normalized.capturedAt,provenance:clone(normalized.provenance),capturedBaseline:clone(normalized.baseline),verifiedBaseline:clone(baseline)};\n  const result=store.commit(clone(normalized.case),{kind:'capture_import',source:normalized.provenance.url},expectedRevision);\n  rememberRevision(result.revision,{value:clone(result.value),pins:[],capture:clone(captureMetadata)});\n  persistMutation(before);\n  render();\n  renderPreview();\n  renderHealth('FAIL');\n  return {status:'IMPORTED',revision:result.revision,case:clone(result.value),baseline:clone(baseline),capture:clone(captureMetadata)};\n}\nfunction resetCase"],
  ["function restore({expectedRevision=revision(),targetRevision}){ store.assertRevision(expectedRevision);const snap=revisions.get(targetRevision);if(!snap)throw new Error('REVISION_NOT_FOUND');const before=snapshotCanonical();pins=new Set(snap.pins||[]);const result=store.commit(snap.value,{kind:'restore',from:targetRevision},expectedRevision);rememberRevision(result.revision,{value:clone(result.value),pins:[...pins]});persistMutation(before);render();renderPreview();return inspect(); }", "function restore({expectedRevision=revision(),targetRevision}){ store.assertRevision(expectedRevision);const snap=revisions.get(targetRevision);if(!snap)throw new Error('REVISION_NOT_FOUND');const before=snapshotCanonical();pins=new Set(snap.pins||[]);captureMetadata=clone(snap.capture??null);const result=store.commit(snap.value,{kind:'restore',from:targetRevision},expectedRevision);rememberRevision(result.revision,{value:clone(result.value),pins:[...pins],capture:clone(captureMetadata)});persistMutation(before);render();renderPreview();return inspect(); }"],
  ["const CASE_SCHEMA={type:'object',additionalProperties:false,properties:{html:{type:'string'},css:{type:'string'},js:{type:'string'},oracle:ORACLE_SCHEMA},required:['html','css','js','oracle']};", "const CASE_SCHEMA={type:'object',additionalProperties:false,properties:{html:{type:'string'},css:{type:'string'},js:{type:'string'},oracle:ORACLE_SCHEMA},required:['html','css','js','oracle']};\nconst CAPTURE_PROVENANCE_SCHEMA={type:'object',additionalProperties:false,properties:{adapter:{type:'string',enum:['faultline-playwright']},adapterVersion:{type:'integer',enum:[1]},url:{type:'string'},title:{type:'string'},userAgent:{type:'string'},viewport:{type:'object',additionalProperties:false,properties:{width:{type:'integer',minimum:1},height:{type:'integer',minimum:1}},required:['width','height']},browser:{type:'string'}},required:['adapter','adapterVersion','url','title','userAgent','viewport','browser']};\nconst CAPTURE_BASELINE_SCHEMA={type:'object',additionalProperties:false,properties:{captured:{type:'boolean',enum:[true]},status:{type:'string',enum:['FAIL']},evidence:{type:'object'},note:{type:'string'}},required:['captured','status','evidence','note']};\nconst CAPTURE_SCHEMA={type:'object',additionalProperties:false,properties:{format:{type:'string',enum:['faultline.capture']},version:{type:'integer',enum:[1]},capturedAt:{type:'string'},case:CASE_SCHEMA,provenance:CAPTURE_PROVENANCE_SCHEMA,baseline:CAPTURE_BASELINE_SCHEMA},required:['format','version','capturedAt','case','provenance','baseline']};"],
  ["['faultline_load_case','Replace the complete canonical HTML, CSS, JavaScript and oracle in one optimistic revision.',{...REVISION_PROPERTY,case:CASE_SCHEMA},async input=>loadCase(input),false,true,['expectedRevision','case']],", "['faultline_load_case','Replace the complete canonical HTML, CSS, JavaScript and oracle in one optimistic revision.',{...REVISION_PROPERTY,case:CASE_SCHEMA},async input=>loadCase(input),false,true,['expectedRevision','case']],\n ['faultline_import_capture','Validate a versioned Playwright capture, independently reproduce its FAIL result inside the deterministic sandbox, then atomically commit source plus provenance.',{...REVISION_PROPERTY,artifact:CAPTURE_SCHEMA},async(input,options)=>importCapture(input,options),false,true,['expectedRevision','artifact']],"],
  ["['faultline_export','Export the current case as a standalone HTML reproducer.',{},async()=>({html:exportCase()}),true,true],", "['faultline_export','Export the current case as a standalone HTML reproducer with capture provenance when available.',{},async()=>({html:exportCase(),capture:clone(captureMetadata)}),true,true],"],
  ["window.faultline={inspect,units,loadCase,resetCase,run,defineOracle,applySource,probe,reduce,pin,history,revisions:listRevisions,restore,exportCase,autopilot,manifest:", "window.faultline={inspect,units,loadCase,importCapture,resetCase,run,defineOracle,applySource,probe,reduce,pin,history,revisions:listRevisions,restore,exportCase,autopilot,manifest:"]
]);

await patch('src/capture-integration.js',[
  ["if(!api||typeof api.loadCase!=='function'||typeof api.inspect!=='function')throw new Error('FAULTLINE_CAPTURE_RUNTIME_UNAVAILABLE');", "if(!api||typeof api.importCapture!=='function'||typeof api.inspect!=='function')throw new Error('FAULTLINE_CAPTURE_RUNTIME_UNAVAILABLE');"],
  ["export function importCapture({artifact,expectedRevision}={}){\n  const normalized=validateCaptureArtifact(artifact);\n  const api=runtime();\n  const revision=expectedRevision??api.inspect().revision;\n  const result=api.loadCase({expectedRevision:revision,case:normalized.case});\n  return {\n    result,\n    capture:{\n      format:normalized.format,\n      version:normalized.version,\n      capturedAt:normalized.capturedAt,\n      provenance:normalized.provenance,\n      baseline:normalized.baseline\n    }\n  };\n}", "export async function importCapture({artifact,expectedRevision}={}){\n  const normalized=validateCaptureArtifact(artifact);\n  const api=runtime();\n  const revision=expectedRevision??api.inspect().revision;\n  return api.importCapture({expectedRevision:revision,artifact:normalized});\n}"]
]);

await patch('src/ui.js',[
  ["const toolCountLabel=({13:'thirteen',14:'fourteen',15:'fifteen',16:'sixteen'})[toolCount]||String(toolCount);", "const toolCountLabel=({13:'thirteen',14:'fourteen',15:'fifteen',16:'sixteen',17:'seventeen'})[toolCount]||String(toolCount);"],
  ["const imported=window.faultlineCapture.import({artifact,expectedRevision:current.revision});", "const imported=await window.faultlineCapture.import({artifact,expectedRevision:current.revision});"],
  ["markReady(`Playwright capture imported at ${imported.result.revision}; baseline must be re-run before reduction.`);", "markReady(`Playwright capture imported at ${imported.revision}; FAIL independently re-verified before commit.`);"]
]);

await patch('tests/browser-e2e.mjs',[
  ["window.__webmcpTools?.length===16", "window.__webmcpTools?.length===17"],
  ["assert.deepEqual(await page.evaluate(()=>Object.keys(window.faultline).sort()),['applySource','autopilot','defineOracle','exportCase','history','inspect','loadCase','manifest','pin','probe','reduce','resetCase','restore','revisions','run','units'].sort());", "assert.deepEqual(await page.evaluate(()=>Object.keys(window.faultline).sort()),['applySource','autopilot','defineOracle','exportCase','history','importCapture','inspect','loadCase','manifest','pin','probe','reduce','resetCase','restore','revisions','run','units'].sort());"],
  ["'WebMCP ready · 16 tools'", "'WebMCP ready · 17 tools'"],
  ["/16 WebMCP tools/i", "/17 WebMCP tools/i"],
  ["assert.equal(toolContract.length,16);", "assert.equal(toolContract.length,17);"],
  ["window.__webmcpTools?.length===16", "window.__webmcpTools?.length===17"]
]);

console.log('capture runtime hardening patch applied');
